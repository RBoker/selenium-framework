<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rboker.automation</groupId>
    <artifactId>selenium-framework</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <!-- ========================================================= -->
    <!-- Properties                                                -->
    <!-- ========================================================= -->
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>17</maven.compiler.release>

        <!-- Selenium -->
        <selenium.version>4.40.0</selenium.version>

        <!-- JUnit -->
        <junit.jupiter.version>5.10.3</junit.jupiter.version>
        <junit.platform.version>1.10.3</junit.platform.version>

        <mockito.version>5.12.0</mockito.version>
        <assertj.version>3.26.3</assertj.version>

        <cucumber.version>7.18.1</cucumber.version>
        <snakeyaml.version>2.2</snakeyaml.version>

        <slf4j.version>2.0.16</slf4j.version>
        <logback.version>1.5.25</logback.version>

        <allure.version>2.29.0</allure.version>
        <allure.maven.version>2.12.0</allure.maven.version>

        <compiler.version>3.13.0</compiler.version>
        <surefire.version>3.5.2</surefire.version>
        <failsafe.version>3.5.2</failsafe.version>
        <jacoco.version>0.8.12</jacoco.version>
        <exec.plugin.version>3.3.0</exec.plugin.version>
        <antrun.version>3.1.0</antrun.version>

        <failsafe.testFailureIgnore>false</failsafe.testFailureIgnore>

        <!-- YAML configurator -->
        <yaml.configurator.mainClass>
            com.rboker.automation.support.ProjectYamlConfigurator
        </yaml.configurator.mainClass>

        <!-- Allure paths -->
        <allure.results.dir>${project.build.directory}/allure-results</allure.results.dir>
        <allure.report.dir>${project.build.directory}/site/allure-maven-plugin</allure.report.dir>

        <!-- Persisted history (OUTSIDE target) -->
        <allure.history.base.dir>${project.basedir}/.allure-history</allure.history.base.dir>

        <!-- Encoding -->
        <file.encoding>UTF-8</file.encoding>
    </properties>

    <!-- ========================================================= -->
    <!-- Dependency Management                                     -->
    <!-- ========================================================= -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-bom</artifactId>
                <version>${cucumber.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ========================================================= -->
    <!-- Dependencies                                              -->
    <!-- ========================================================= -->
    <dependencies>

        <!-- Selenium -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- YAML -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>${snakeyaml.version}</version>
        </dependency>

        <!-- Cucumber -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- PicoContainer -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-picocontainer</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- JUnit -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Suite API/Engine -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite-api</artifactId>
            <version>${junit.platform.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite-engine</artifactId>
            <version>${junit.platform.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Mockito -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- AssertJ -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Allure (Cucumber) -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-cucumber7-jvm</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <!-- ========================================================= -->
    <!-- Build                                                     -->
    <!-- ========================================================= -->
    <build>
        <plugins>

            <!-- Compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>

            <!-- JaCoCo -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <propertyName>argLine</propertyName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Surefire (UNIT) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <argLine>${argLine} -Dfile.encoding=${file.encoding} -Dsun.jnu.encoding=${file.encoding}</argLine>
                    <includes>
                        <include>**/*UnitTest.java</include>
                    </includes>
                    <rerunFailingTestsCount>0</rerunFailingTestsCount>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <!-- ========================================================= -->
    <!-- Profile UI                                                -->
    <!-- ========================================================= -->
    <profiles>

        <profile>
            <id>ui</id>

            <build>
                <plugins>

                    <!-- YAML → junit-platform.properties -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>${exec.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>generate-junit-platform-properties-from-yaml</id>
                                <phase>process-test-classes</phase>
                                <goals>
                                    <goal>java</goal>
                                </goals>
                                <configuration>
                                    <mainClass>${yaml.configurator.mainClass}</mainClass>
                                    <classpathScope>test</classpathScope>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Restore + Save Allure history (Ant) -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>${antrun.version}</version>
                        <executions>

                            <!-- RESTORE -->
                            <execution>
                                <id>allure-history-restore</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target name="allure-history-restore">

                                        <condition property="allure.project.id" value="${project}" else="default">
                                            <and>
                                                <isset property="project"/>
                                                <not>
                                                    <equals arg1="${project}" arg2=""/>
                                                </not>
                                            </and>
                                        </condition>

                                        <echo message="[ALLURE] allure.project.id=${allure.project.id}"/>
                                        <echo message="[ALLURE] resultsDir=${allure.results.dir}"/>
                                        <echo message="[ALLURE] historyStore=${allure.history.base.dir}/${allure.project.id}/history"/>

                                        <mkdir dir="${allure.results.dir}"/>
                                        <mkdir dir="${allure.results.dir}/history"/>

                                        <!-- categories.json (opcional) -->
                                        <copy file="${project.basedir}/src/test/resources/categories.json"
                                              tofile="${allure.results.dir}/categories.json"
                                              overwrite="true"
                                              failonerror="false"/>

                                        <!-- history (opcional): copia o que existir, sem falhar -->
                                        <copy todir="${allure.results.dir}/history"
                                              overwrite="true"
                                              failonerror="false">
                                            <fileset dir="${allure.history.base.dir}/${allure.project.id}/history"
                                                     erroronmissingdir="false"/>
                                        </copy>

                                    </target>
                                </configuration>
                            </execution>

                            <!-- SAVE (depois do report) -->
                            <execution>
                                <id>allure-history-save</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target name="allure-history-save">

                                        <condition property="allure.project.id" value="${project}" else="default">
                                            <and>
                                                <isset property="project"/>
                                                <not>
                                                    <equals arg1="${project}" arg2=""/>
                                                </not>
                                            </and>
                                        </condition>

                                        <echo message="[ALLURE] allure.project.id=${allure.project.id}"/>
                                        <echo message="[ALLURE] saveFrom=${allure.report.dir}/history"/>
                                        <echo message="[ALLURE] saveTo=${allure.history.base.dir}/${allure.project.id}/history"/>

                                        <mkdir dir="${allure.history.base.dir}/${allure.project.id}/history"/>

                                        <!-- copia o history do report se existir (sem falhar se não existir) -->
                                        <copy todir="${allure.history.base.dir}/${allure.project.id}/history"
                                              overwrite="true"
                                              failonerror="false">
                                            <fileset dir="${allure.report.dir}/history"
                                                     erroronmissingdir="false"/>
                                        </copy>

                                    </target>
                                </configuration>
                            </execution>

                        </executions>
                    </plugin>

                    <!-- Failsafe (UI / Cucumber) -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${failsafe.version}</version>
                        <configuration>
                            <useModulePath>false</useModulePath>
                            <testFailureIgnore>${failsafe.testFailureIgnore}</testFailureIgnore>

                            <argLine>${argLine} -Dfile.encoding=${file.encoding} -Dsun.jnu.encoding=${file.encoding}</argLine>

                            <systemPropertyVariables>
                                <allure.results.directory>${allure.results.dir}</allure.results.directory>
                            </systemPropertyVariables>

                            <rerunFailingTestsCount>0</rerunFailingTestsCount>
                            <includes>
                                <include>**/UiTestRunner.java</include>
                            </includes>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Allure report (pós ITs) -->
                    <plugin>
                        <groupId>io.qameta.allure</groupId>
                        <artifactId>allure-maven</artifactId>
                        <version>${allure.maven.version}</version>
                        <configuration>
                            <resultsDirectory>${allure.results.dir}</resultsDirectory>
                            <reportDirectory>${allure.report.dir}</reportDirectory>
                        </configuration>
                        <executions>
                            <execution>
                                <id>allure-report</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Disable unit tests in UI profile -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${surefire.version}</version>
                        <configuration>
                            <skipTests>true</skipTests>
                        </configuration>
                    </plugin>

                </plugins>
            </build>
        </profile>

    </profiles>

</project>
