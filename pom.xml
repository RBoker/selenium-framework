<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rboker.automation</groupId>
    <artifactId>selenium-framework</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>17</maven.compiler.release>

        <selenium.version>4.25.0</selenium.version>

        <junit.jupiter.version>5.10.3</junit.jupiter.version>
        <junit.platform.version>1.10.3</junit.platform.version>

        <mockito.version>5.12.0</mockito.version>
        <assertj.version>3.26.3</assertj.version>

        <cucumber.version>7.18.1</cucumber.version>

        <snakeyaml.version>2.2</snakeyaml.version>

        <slf4j.version>2.0.16</slf4j.version>
        <logback.version>1.5.25</logback.version>

        <allure.version>2.29.0</allure.version>
        <allure.maven.version>2.12.0</allure.maven.version>

        <compiler.version>3.13.0</compiler.version>
        <surefire.version>3.5.2</surefire.version>
        <failsafe.version>3.5.2</failsafe.version>
        <jacoco.version>0.8.12</jacoco.version>
        <exec.plugin.version>3.3.0</exec.plugin.version>

        <!-- Antrun para restore/save do history do Allure -->
        <antrun.version>3.1.0</antrun.version>

        <failsafe.testFailureIgnore>false</failsafe.testFailureIgnore>

        <!-- Classe real (package correto) -->
        <yaml.configurator.mainClass>com.rboker.automation.support.ProjectYamlConfigurator</yaml.configurator.mainClass>

        <junit.platform.props.out>${project.build.testOutputDirectory}/junit-platform.properties</junit.platform.props.out>
        <allure.results.dir>${project.build.directory}/allure-results</allure.results.dir>

        <!-- diretório persistente do history por projeto (fora do target) -->
        <allure.history.dir>${project.basedir}/.allure-history</allure.history.dir>

        <!-- NEW: diretório do report (onde o Allure gera o history) -->
        <allure.report.dir>${project.build.directory}/site/allure-maven-plugin</allure.report.dir>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-bom</artifactId>
                <version>${cucumber.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- Selenium -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- YAML (COMPILA no main também) -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>${snakeyaml.version}</version>
        </dependency>

        <!-- Cucumber -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- JUnit Jupiter -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- JUnit Platform Suite -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite-api</artifactId>
            <version>${junit.platform.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite-engine</artifactId>
            <version>${junit.platform.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Mockito -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- AssertJ -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Allure -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-cucumber7-jvm</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-junit-platform</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!-- Compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <!-- JaCoCo -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <propertyName>argLine</propertyName>
                            <destFile>${project.build.directory}/jacoco.exec</destFile>
                            <append>true</append>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Surefire (UNIT) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <useModulePath>false</useModulePath>
                    <argLine>@{argLine} -Dfile.encoding=UTF-8 -Djava.util.logging.config.file="${project.basedir}/src/test/resources/logging.properties"</argLine>
                    <includes>
                        <include>**/*UnitTest.java</include>
                    </includes>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <profiles>

        <profile>
            <id>ui</id>
            <build>
                <plugins>

                    <!-- 1) RESTORE history antes dos testes (Trend) -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>${antrun.version}</version>
                        <executions>

                            <execution>
                                <id>allure-history-restore</id>
                                <phase>pre-integration-test</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <property name="projectId" value="${project}"/>

                                        <condition property="projectId" value="default" else="${projectId}">
                                            <equals arg1="${projectId}" arg2=""/>
                                        </condition>

                                        <property name="historySource" value="${allure.history.dir}/${projectId}/history"/>
                                        <property name="historyTarget" value="${allure.results.dir}/history"/>

                                        <echo message="[AllureHistory] Restore: ${historySource} -> ${historyTarget}"/>

                                        <mkdir dir="${allure.results.dir}"/>

                                        <copy todir="${historyTarget}" overwrite="true" verbose="true" failonerror="false">
                                            <fileset dir="${historySource}" includes="**/*"/>
                                        </copy>
                                    </target>
                                </configuration>
                            </execution>

                            <!-- 4) SAVE history no final: copia do REPORT (onde o Allure realmente gera history/) -->
                            <execution>
                                <id>allure-history-save</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <property name="projectId" value="${project}"/>

                                        <condition property="projectId" value="default" else="${projectId}">
                                            <equals arg1="${projectId}" arg2=""/>
                                        </condition>

                                        <property name="historyFromReport" value="${allure.report.dir}/history"/>
                                        <property name="historyDest" value="${allure.history.dir}/${projectId}/history"/>

                                        <echo message="[AllureHistory] Save: ${historyFromReport} -> ${historyDest}"/>

                                        <mkdir dir="${historyDest}"/>

                                        <copy todir="${historyDest}" overwrite="true" verbose="true" failonerror="false">
                                            <fileset dir="${historyFromReport}" includes="**/*"/>
                                        </copy>
                                    </target>
                                </configuration>
                            </execution>

                        </executions>
                    </plugin>

                    <!-- 2) Gera junit-platform.properties a partir do YAML -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>${exec.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>generate-junit-platform-properties-from-yaml</id>
                                <phase>process-test-classes</phase>
                                <goals>
                                    <goal>java</goal>
                                </goals>
                                <configuration>
                                    <mainClass>${yaml.configurator.mainClass}</mainClass>
                                    <classpathScope>test</classpathScope>

                                    <systemProperties>
                                        <systemProperty>
                                            <key>allure.results.directory</key>
                                            <value>${allure.results.dir}</value>
                                        </systemProperty>
                                    </systemProperties>

                                    <cleanupDaemonThreads>false</cleanupDaemonThreads>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Failsafe -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${failsafe.version}</version>
                        <configuration>
                            <useModulePath>false</useModulePath>
                            <testFailureIgnore>${failsafe.testFailureIgnore}</testFailureIgnore>
                            <argLine>@{argLine} -Dfile.encoding=UTF-8 -Djava.util.logging.config.file="${project.basedir}/src/test/resources/logging.properties"</argLine>
                            <systemPropertyVariables>
                                <allure.results.directory>${allure.results.dir}</allure.results.directory>
                            </systemPropertyVariables>
                            <includes>
                                <include>**/RunCucumberTest.java</include>
                            </includes>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- 3) Allure report: precisa rodar ANTES do save do history -->
                    <plugin>
                        <groupId>io.qameta.allure</groupId>
                        <artifactId>allure-maven</artifactId>
                        <version>${allure.maven.version}</version>
                        <executions>
                            <execution>
                                <id>allure-report-post-it</id>
                                <phase>post-integration-test</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Disable unit tests -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${surefire.version}</version>
                        <configuration>
                            <skipTests>true</skipTests>
                        </configuration>
                    </plugin>

                </plugins>
            </build>
        </profile>

    </profiles>

</project>
